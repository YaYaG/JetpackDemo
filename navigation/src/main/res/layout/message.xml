<?xml version="1.0" encoding="utf-8"?>
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical">


    <TextView
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:text="例如，如果通过 Activity 的 onCreateOptionsMenu() 添加菜单，则您可以通过替换 Activity 的 onOptionsItemSelected() 以调用 onNavDestinationSelected() 来将菜单项与目的地相关联，如下所示例如，如果通过 Activity 的 onCreateOptionsMenu() 添加菜单，则您可以通过替换 Activity 的 onOptionsItemSelected() 以调用 onNavDestinationSelected() 来将菜单项与目的地相关联，如下所示例如，如果通过 Activity 的 onCreateOptionsMenu() 添加菜单，则您可以通过替换 Activity 的 onOptionsItemSelected() 以调用 onNavDestinationSelected() 来将菜单项与目的地相关联，如下所示例如，如果通过 Activity 的 onCreateOptionsMenu() 添加菜单，则您可以通过替换 Activity 的 onOptionsItemSelected() 以调用 onNavDestinationSelected() 来将菜单项与目的地相关联，如下所示例如，如果通过 Activity 的 onCreateOptionsMenu() 添加菜单，则您可以通过替换 Activity 的 onOptionsItemSelected() 以调用 onNavDestinationSelected() 来将菜单项与目的地相关联，如下所示例如，如果通过 Activity 的 onCreateOptionsMenu() 添加菜单，则您可以通过替换 Activity 的 onOptionsItemSelected() 以调用 onNavDestinationSelected() 来将菜单项与目的地相关联，如下所示例如，如果通过 Activity 的 onCreateOptionsMenu() 添加菜单，则您可以通过替换 Activity 的 onOptionsItemSelected() 以调用 onNavDestinationSelected() 来将菜单项与目的地相关联，如下所示例如，如果通过 Activity 的 onCreateOptionsMenu() 添加菜单，则您可以通过替换 Activity 的 onOptionsItemSelected() 以调用 onNavDestinationSelected() 来将菜单项与目的地相关联，如下所示例如，如果通过 Activity 的 onCreateOptionsMenu() 添加菜单，则您可以通过替换 Activity 的 onOptionsItemSelected() 以调用 onNavDestinationSelected() 来将菜单项与目的地相关联，如下所示例如，如果通过 Activity 的 onCreateOptionsMenu() 添加菜单，则您可以通过替换 Activity 的 onOptionsItemSelected() 以调用 onNavDestinationSelected() 来将菜单项与目的地相关联，如下所示例如，如果通过 Activity 的 onCreateOptionsMenu() 添加菜单，则您可以通过替换 Activity 的 onOptionsItemSelected() 以调用 onNavDestinationSelected() 来将菜单项与目的地相关联，如下所示例如，如果通过 Activity 的 onCreateOptionsMenu() 添加菜单，则您可以通过替换 Activity 的 onOptionsItemSelected() 以调用 onNavDestinationSelected() 来将菜单项与目的地相关联，如下所示例如，如果通过 Activity 的 onCreateOptionsMenu() 添加菜单，则您可以通过替换 Activity 的 onOptionsItemSelected() 以调用 onNavDestinationSelected() 来将菜单项与目的地相关联，如下所示例如，如果通过 Activity 的 onCreateOptionsMenu() 添加菜单，则您可以通过替换 Activity 的 onOptionsItemSelected() 以调用 onNavDestinationSelected() 来将菜单项与目的地相关联，如下所示例如，如果通过 Activity 的 onCreateOptionsMenu() 添加菜单，则您可以通过替换 Activity 的 onOptionsItemSelected() 以调用 onNavDestinationSelected() 来将菜单项与目的地相关联，如下所示例如，如果通过 Activity 的 onCreateOptionsMenu() 添加菜单，则您可以通过替换 Activity 的 onOptionsItemSelected() 以调用 onNavDestinationSelected() 来将菜单项与目的地相关联，如下所示例如，如果通过 Activity 的 onCreateOptionsMenu() 添加菜单，则您可以通过替换 Activity 的 onOptionsItemSelected() 以调用 onNavDestinationSelected() 来将菜单项与目的地相关联，如下所示例如，如果通过 Activity 的 onCreateOptionsMenu() 添加菜单，则您可以通过替换 Activity 的 onOptionsItemSelected() 以调用 onNavDestinationSelected() 来将菜单项与目的地相关联，如下所示例如，如果通过 Activity 的 onCreateOptionsMenu() 添加菜单，则您可以通过替换 Activity 的 onOptionsItemSelected() 以调用 onNavDestinationSelected() 来将菜单项与目的地相关联，如下所示例如，如果通过 Activity 的 onCreateOptionsMenu() 添加菜单，则您可以通过替换 Activity 的 onOptionsItemSelected() 以调用 onNavDestinationSelected() 来将菜单项与目的地相关联，如下所示例如，如果通过 Activity 的 onCreateOptionsMenu() 添加菜单，则您可以通过替换 Activity 的 onOptionsItemSelected() 以调用 onNavDestinationSelected() 来将菜单项与目的地相关联，如下所示例如，如果通过 Activity 的 onCreateOptionsMenu() 添加菜单，则您可以通过替换 Activity 的 onOptionsItemSelected() 以调用 onNavDestinationSelected() 来将菜单项与目的地相关联，如下所示例如，如果通过 Activity 的 onCreateOptionsMenu() 添加菜单，则您可以通过替换 Activity 的 onOptionsItemSelected() 以调用 onNavDestinationSelected() 来将菜单项与目的地相关联，如下所示例如，如果通过 Activity 的 onCreateOptionsMenu() 添加菜单，则您可以通过替换 Activity 的 onOptionsItemSelected() 以调用 onNavDestinationSelected() 来将菜单项与目的地相关联，如下所示例如，如果通过 Activity 的 onCreateOptionsMenu() 添加菜单，则您可以通过替换 Activity 的 onOptionsItemSelected() 以调用 onNavDestinationSelected() 来将菜单项与目的地相关联，如下所示" />

</ScrollView>